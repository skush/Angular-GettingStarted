//to delete:
// created for taking notes =at Jump Start
ng new lesson-planner
ng generate component ...__dirname


file '.angular-cli.json':
prefix: "pm" ("app")

<nav>
    <a> Lesson Planneer </a>
</nav>

const routes: Routes = [
    { path: "lessons", component: LessonsListComponent }
];

<a roterLink="settings" roterLinkActive="selected-menu-item">

//правильно:
// нужно чтобі компоненты были тегами, а деректовы - атрибутами